/**
 * Requiring all modules that we need in order to make AFrame work.
 */

// Aliased in webpack config to aframe/src/index.js to ensure a single AFrame instance
import AFrame from 'aframe';

export default AFrame;
